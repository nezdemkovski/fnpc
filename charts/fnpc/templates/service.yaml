apiVersion: v1
kind: Service
metadata:
  name: {{ include "fnpc.fullname" . }}
  labels:
    {{- include "fnpc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fnpc.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
